# لوحة تحكم المطور - محافظي SaaS

## 📊 نظرة عامة

لوحة تحكم شاملة للمطورين لإدارة نظام محافظي SaaS متعدد المستأجرين.

## ✨ الميزات

### 1. **نظرة عامة على النظام**
- معلومات النظام الأساسية (الاسم، الإصدار، البيئة)
- بطاقات ملخصة للمستأجرين والشركات والمستخدمين
- إحصائيات مالية شاملة (إجمالي الأرصدة، الدخل، المصروفات)
- آخر النشاطات (المستأجرين والمعاملات)
- توزيع البيانات (أدوار المستخدمين، أنواع المحافظ)

### 2. **إدارة المستأجرين**
- عرض جميع المستأجرين مع إحصائياتهم
- تبديل حالة المستأجر (نشط/غير نشط)
- تغيير خطة الاشتراك (مجاني/تاجر)
- حذف المستأجرين مع جميع بياناتهم
- عرض محافظ كل مستأجر
- عرض شركات كل مستأجر

### 3. **إدارة قاعدة البيانات**
- معلومات قاعدة البيانات (النوع، الإصدار)
- إحصائيات السجلات لكل نموذج
- عرض تفصيلي لعدد السجلات
- أداة حذف جميع البيانات (بطلب تأكيد)

### 4. **فحص صحة النظام**
- حالة قاعدة البيانات
- زمن الاستجابة
- وقت التشغيل
- معلومات السيرفر (Node.js، المنصة، الذاكرة)

## 🌐 الوصول إلى اللوحة

- **رابط التطبيق**: `http://localhost:3000`
- **رابط لوحة المطور**: `http://localhost:3000/dev`
- **التنقل**: من التطبيق الرئيسي، اضغط على زر "لوحة تحكم المطور" في الـ footer

## 📡 APIs الخاصة بالمطور

### إحصائيات النظام
```
GET /api/dev/stats
```
**الرد يتضمن:**
- معلومات النظام
- إجمالي السجلات لكل نموذج
- توزيع الخطط
- الملخص المالي
- توزيع البيانات
- آخر النشاطات

### فحص الصحة
```
GET /api/dev/health
```
**الرد يتضمن:**
- حالة النظام العامة
- فحص صحة قاعدة البيانات
- زمن الاستجابة
- وقت التشغيل
- معلومات السيرفر
- استخدام الذاكرة

### إدارة المستأجرين
```
GET    /api/dev/tenants
PATCH  /api/dev/tenants
DELETE /api/dev/tenants
```

**العمليات:**
- `GET`: عرض جميع المستأجرين مع تفاصيلهم الكاملة
- `PATCH`: تحديث حالة المستأجر أو خطة الاشتراك
- `DELETE`: حذف مستأجر وجميع بياناته

### إدارة قاعدة البيانات
```
GET    /api/dev/database
DELETE /api/dev/database
```

**العمليات:**
- `GET`: عرض معلومات قاعدة البيانات
- `DELETE`: حذف جميع البيانات (يتطلب تأكيد خاص)

## 🎨 واجهة المستخدم

### التصميم
- تصميم dark mode احترافي
- ألوان متدرجة (slate-900 to slate-800)
- بطاقات ملونة للإحصائيات
- أيقونات من Lucide React

### التنقل
- أربعة تبويب رئيسية:
  1. **نظرة عامة**: إحصائيات النظام
  2. **المستأجرون**: إدارة المستأجرين
  3. **قاعدة البيانات**: معلومات وإدارة قاعدة البيانات
  4. **النظام**: فحص صحة النظام

### التحديث التلقائي
- تحديث تلقائي كل 30 ثانية
- زر تحديث يدوي
- مؤشر حالة النظام

## 🛡️ الأمان

### التحذيرات
- منطقة خطرة واضحة لحذف البيانات
- تأكيد مزدوج قبل حذف جميع البيانات
- رسائل تحذير ملونة

### الصلاحيات
- يتم استخدام الـ APIs فقط من جانب السيرفر
- لا يوجد مصادقة حالياً (للتطوير)
- في الإنتاج: يجب إضافة JWT أو API Keys

## 📊 الإحصائيات المعروضة

### المستأجرين
- عدد المستأجرين الكلي
- المستأجرين المجانيين
- المستأجرين التجار
- المستأجرين النشطين

### الشركات والفروع
- عدد الشركات
- عدد الفروع
- المستخدمين لكل شركة

### المستخدمين
- عدد المستخدمين الكلي
- التوزيع حسب الدور
- المستخدمين لكل فرع

### المحافظ
- عدد المحافظ
- التوزيع حسب النوع
- إجمالي الأرصدة

### المعاملات
- عدد المعاملات
- آخر المعاملات
- التدفق المالي (دخل/مصروف)

## ⚠️ عمليات خطرة

### حذف جميع البيانات
**المسار:**
```
POST /api/dev/database
Body: { "confirm": "DELETE_ALL_DATA" }
```

**يحذف:**
- جميع المستأجرين
- جميع الشركات والفروع
- جميع المستخدمين والشركاء
- جميع المحافظ والمعاملات
- جميع التصنيفات

**مطلوب:**
- تأكيد يدوي بالنص المحدد
- تنبيهات واضحة في الواجهة

## 🔧 أدوات التطوير

### فحص المشاكل
- فحص صحة قاعدة البيانات
- مراقبة الأداء (زمن الاستجابة)
- مراقبة استخدام الذاكرة
- عرض الأخطاء في الكونسول

### إدارة البيانات
- حذف البيانات التجريبية
- إعادة تعيين النظام
- مشاهدة إحصائيات السجلات

## 🚀 التشغيل

### بيئة التطوير
```bash
# الخادم يعمل تلقائياً على http://localhost:3000
# للوصول للوحة المطور:
# http://localhost:3000/dev
```

### فحص الأداء
```bash
# مراقبة السجلات
tail -f /home/z/my-project/dev.log

# فحص الأخطاء
bun run lint
```

## 📝 التطوير المستقبلي

### الميزات المخطط لها:
- [ ] نظام مصادقة للمطورين
- [ ] سجلات النشاطات التفصيلية
- [ ] تنبيهات المشاكل
- [ ] نسخ احتياطية تلقائية
- [ ] رسوم بيانية للأداء
- [ ] تحليلات متقدمة
- [ ] export البيانات
- [ ] API documentation تفصيلي

## 🤝 المساهمة

لتطوير لوحة تحكم المطور:
1. إضافة endpoints جديدة في `/src/app/api/dev/`
2. إضافة مكونات جديدة في `/src/components/`
3. تحديث `/src/components/DeveloperDashboard.tsx`
4. اختبار التغييرات

## 📚 الوثائق التقنية

### البنية
```
src/
├── app/
│   ├── api/
│   │   └── dev/
│   │       ├── stats/route.ts      # إحصائيات النظام
│   │       ├── health/route.ts     # فحص الصحة
│   │       ├── tenants/route.ts    # إدارة المستأجرين
│   │       └── database/route.ts  # إدارة قاعدة البيانات
│   ├── dev/
│   │   └── page.tsx              # صفحة لوحة المطور
│   └── page.tsx                  # صفحة التطبيق الرئيسية
└── components/
    ├── DeveloperDashboard.tsx       # مكون لوحة المطور
    └── ...
```

### APIs الخارجية
- `GET /api/dev/stats` - إحصائيات النظام
- `GET /api/dev/health` - فحص الصحة
- `GET/PATCH/DELETE /api/dev/tenants` - إدارة المستأجرين
- `GET/DELETE /api/dev/database` - إدارة قاعدة البيانات

## 🎯 حالات الاستخدام

### 1. **مراقبة الأداء**
- افتح لوحة المطور
- راقب زمن الاستجابة
- راقب استخدام الذاكرة

### 2. **إدارة المستأجرين**
- افتح تبويب المستأجرين
- عرض إحصائيات كل مستأجر
- تبديل حالة أو حذف المستأجرين

### 3. **الصيانة**
- افتح تبويب النظام
- فحص صحة قاعدة البيانات
- حذف البيانات التجريبية

### 4. **التحليل**
- افتح تبويب نظرة عامة
- راقب التوزيعات
- تحليل النشاطات

---

تم التطوير بواسطة ❤️ باستخدام Next.js، TypeScript، و Prisma
