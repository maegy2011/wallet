# محافظي SaaS - نظام إدارة المحافظ الإلكترونية متعدد المستأجرين

تطبيق SaaS متكامل لإدارة المحافظ الإلكترونية بأسلوب Multi-Tenant، مصمم للإيجار لعدة مستأجرين مع دعم الشركات والفروع المتعددة.

## 🏢 الميزات الأساسية للنظام المتعدد المستأجرين

### هيكلية النظام

```
المستأجر (Tenant)
  ├── الشركات (Companies)
  │     ├── الفروع (Branches)
  │     │     ├── المستخدمون (Users)
  │     │     └── المحافظ (Wallets)
  │     └── المستخدمون (Users)
  ├── المستخدمون (Users)
  ├── الشركاء (Partners)
  ├── المحافظ (Wallets)
  ├── التصنيفات (Categories)
  └── المعاملات (Transactions)
```

### 1. **نظام المستأجرين (Tenants)**
- كل مستأجر بياناته معزولة تماماً عن باقي المستأجرين
- خطط اشتراك مرنة (مجانية وتاجر)
- معلومات الأعمال والسجل التجاري
- تواريخ الاشتراك والتجديد

### 2. **نظام الشركات والفروع**
- **الشركات**: إضافة عدة شركات لكل مستأجر
- **الفروع**: إنشاء فروع متعددة لكل شركة
- **العزل التام**: كل فرع له محافظ ومستخدمين مستقلين

### 3. **نظام المستخدمين والأدوار**

#### الأدوار المتاحة:
- **مالك المستأجر (TENANT_OWNER)**: وصول كامل لجميع بيانات المستأجر
- **مدير شركة (COMPANY_MANAGER)**: إدارة شركة واحدة وفروعها
- **مدير فرع (BRANCH_MANAGER)**: إدارة فرع واحد ومحافظه وموظفيه
- **مشرف (SUPERVISOR)**: مراقبة العمليات ضمن الفرع
- **موظف (EMPLOYEE)**: تنفيذ المهام المحددة

### 4. **نظام الشركاء التجاريين**
- إضافة شركاء تجاريين (موردين، عملاء، إلخ)
- معلومات الاتصال الكاملة
- تصنيف الأدوار
- إمكانية إضافة ملاحظات

### 5. **نظام المحافظ الإلكترونية**
- إنشاء محافظ متعددة (حسب خطة الاشتراك)
- ربط المحافظ بالفروع
- أنواع المحافظ (عامة، توفير، استثمار، ائتمان)
- تخصيص الألوان والأيقونات
- دعم متعدد العملات

### 6. **تتبع المعاملات**
- إضافة معاملات دخل ومصروف
- تصنيف المعاملات
- تسجيل التواريخ والملاحظات
- حالة المعاملة (مكتملة، معلقة، ملغاة)
- ربط المعاملات بالمالك (المستخدم الذي أنشأها)

### 7. **نظام التصنيفات المالية**
- تصنيفات مخصصة لكل مستأجر
- تصنيفات دخل ومصروف
- تخصيص الأيقونات والألوان
- عزل تام بين المستأجرين

## 💰 خطط الاشتراك (Subscription Plans)

### الخطة المجانية (FREE)
- ✅ 2 محفظة إلكترونية كحد أقصى
- ✅ الوظائف والمميزات الأساسية
- ✅ عدد غير محدود من الشركات والفروع
- ✅ عدد غير محدود من المستخدمين
- ✅ عدد غير محدود من الشركاء
- ✅ تصنيفات مالية مخصصة
- ❌ غير مدعوم: الوظائف المتقدمة

### خطة التاجر (MERCHANT)
- ✅ محافظ إلكترونية غير محدودة
- ✅ جميع الوظائف والمميزات الأساسية
- ✅ الوظائف والمميزات المتقدمة
- ✅ عدد غير محدود من الشركات والفروع
- ✅ عدد غير محدود من المستخدمين
- ✅ عدد غير محدود من الشركاء
- ✅ تصنيفات مالية مخصصة
- ✅ تقارير متقدمة ورسوم بيانية (قريباً)
- ✅ تصدير البيانات (قريباً)
- ✅ دعم الأولوية (قريباً)

## 🔐 عزل البيانات (Data Isolation)

النظام يضمن عزل تام للبيانات بين المستأجرين:
- كل مستأجر له بياناته الخاصة
- لا يمكن للمستأجر رؤية بيانات مستأجر آخر
- كل محفظة مرتبطة بمستأجر واحد فقط
- كل معاملة مرتبطة بمستأجر واحد فقط
- كل تصنيف خاص بمستأجر واحد فقط
- كل شريك تابع لمستأجر واحد فقط

## 📊 التقنيات المستخدمة

### Frontend
- **Next.js 15** مع App Router
- **TypeScript 5**
- **Tailwind CSS 4**
- **shadcn/ui** للمكونات الجاهزة
- **React 19**
- **Lucide React** للأيقونات

### Backend
- **Next.js API Routes**
- **Prisma ORM** مع SQLite
- **TypeScript**

### قاعدة البيانات
- **SQLite** للتخزين المحلي
- **Prisma** كـ ORM
- **Multi-Tenant Architecture**

## 🗄️ هيكل قاعدة البيانات

### Tenant (المستأجر)
```typescript
{
  id: string
  name: string
  email: string (unique)
  phone?: string
  businessName?: string
  businessLicense?: string
  isActive: boolean
  plan: 'FREE' | 'MERCHANT'
  subscriptionEnd?: DateTime
  // Relations
  users: User[]
  companies: Company[]
  partners: Partner[]
  wallets: Wallet[]
  categories: Category[]
  transactions: Transaction[]
}
```

### Company (الشركة)
```typescript
{
  id: string
  name: string
  description?: string
  taxId?: string
  isActive: boolean
  tenantId: string (Foreign Key)
  // Relations
  tenant: Tenant
  branches: Branch[]
  users: User[]
}
```

### Branch (الفرع)
```typescript
{
  id: string
  name: string
  address?: string
  phone?: string
  isActive: boolean
  companyId: string (Foreign Key)
  // Relations
  company: Company
  users: User[]
  wallets: Wallet[]
}
```

### User (المستخدم)
```typescript
{
  id: string
  email: string (unique)
  name?: string
  phone?: string
  password?: string
  role: 'TENANT_OWNER' | 'COMPANY_MANAGER' | 'BRANCH_MANAGER' | 'SUPERVISOR' | 'EMPLOYEE'
  isActive: boolean
  tenantId: string (Foreign Key)
  companyId?: string (Foreign Key)
  branchId?: string (Foreign Key)
  // Relations
  tenant: Tenant
  company?: Company
  branch?: Branch
  transactions: Transaction[]
}
```

### Wallet (المحفظة)
```typescript
{
  id: string
  name: string
  description?: string
  balance: number
  currency: string
  type: string
  icon: string
  color: string
  isActive: boolean
  tenantId: string (Foreign Key)
  branchId?: string (Foreign Key)
  // Relations
  tenant: Tenant
  branch?: Branch
  transactions: Transaction[]
}
```

### Category (التصنيف)
```typescript
{
  id: string
  name: string
  description?: string
  type: string ('income' | 'expense')
  icon?: string
  color?: string
  isActive: boolean
  tenantId: string (Foreign Key)
  // Relations
  tenant: Tenant
  transactions: Transaction[]
}
```

### Transaction (المعاملة)
```typescript
{
  id: string
  title: string
  description?: string
  amount: number
  type: string ('income' | 'expense')
  date: DateTime
  status: string
  tags?: string
  tenantId: string (Foreign Key)
  walletId: string (Foreign Key)
  categoryId?: string (Foreign Key)
  createdById?: string (Foreign Key)
  // Relations
  tenant: Tenant
  wallet: Wallet
  category?: Category
  createdBy?: User
}
```

### Partner (الشريك)
```typescript
{
  id: string
  name: string
  email?: string
  phone?: string
  company?: string
  role?: string
  notes?: string
  isActive: boolean
  tenantId: string (Foreign Key)
  // Relations
  tenant: Tenant
}
```

## 🚀 التثبيت والتشغيل

### المتطلبات
- Node.js 18+ أو Bun
- Git

### خطوات التثبيت

1. **استنساخ المشروع**
```bash
git clone <repository-url>
cd wallet-saas-app
```

2. **تثبيت الحزم**
```bash
bun install
# أو
npm install
```

3. **إعداد قاعدة البيانات**
```bash
bun run db:push
```

4. **تهيئة البيانات الأولية**
```bash
bun run db:seed
```

5. **تشغيل التطبيق**
```bash
bun run dev
```

6. **فتح المتصفح**
افتح [http://localhost:3000](http://localhost:3000)

## 📝 الأوامر المتاحة

```bash
# تشغيل خادم التطوير
bun run dev

# بناء التطبيق للإنتاج
bun run build

# تشغيل خادم الإنتاج
bun run start

# فحص الكود
bun run lint

# تحديث قاعدة البيانات
bun run db:push

# إنشاء Prisma Client
bun run db:generate

# تهيئة البيانات الأولية
bun run db:seed

# إعادة تعيين قاعدة البيانات
bun run db:push --force-reset
```

## 🎨 واجهة المستخدم

### الصفحة الرئيسية - نظرة عامة
- اختيار المستأجر من القائمة
- عرض بطاقات ملخصة (إجمالي الرصيد، الدخل، المصروفات)
- بطاقات المحافظ مع الأرصدة
- آخر 10 معاملات
- تنبيهات حول حدود الخطة

### صفحة الشركات والفروع
- عرض جميع الشركات للمستأجر
- عرض فروع كل شركة
- إنشاء شركات وفروع جديدة
- عرض عدد المحافظ والموظفين لكل شركة

### صفحة المحافظ
- عرض تفصيلي لجميع المحافظ
- معلومات كاملة لكل محفظة
- عدد المعاملات في كل محفظة
- عرض الفرع المرتبط بالمحفظة
- تنبيهات حول حدود الخطة

### صفحة المستخدمين
- عرض جميع المستخدمين
- تصنيف حسب الدور
- عرض الشركة والفرع لكل مستخدم
- إضافة مستخدمين جدد بأدوار مختلفة

### صفحة الشركاء
- عرض جميع الشركاء التجاريين
- معلومات الاتصال الكاملة
- إنشاء شركاء جدد

## 🔌 واجهة برمجة التطبيقات (APIs)

### Tenants APIs
```typescript
GET    /api/tenants              // عرض جميع المستأجرين
POST   /api/tenants              // إنشاء مستأجر جديد
```

### Companies APIs
```typescript
GET    /api/companies?tenantId={id}    // عرض شركات المستأجر
POST   /api/companies                 // إنشاء شركة جديدة
```

### Branches APIs
```typescript
GET    /api/branches?tenantId={id}    // عرض فروع المستأجر
GET    /api/branches?companyId={id}    // عرض فروع شركة
POST   /api/branches                 // إنشاء فرع جديد
```

### Users APIs
```typescript
GET    /api/users?tenantId={id}      // عرض مستخدمي المستأجر
GET    /api/users?companyId={id}      // عرض مستخدمي شركة
GET    /api/users?branchId={id}       // عرض مستخدمي فرع
POST   /api/users                   // إضافة مستخدم جديد
```

### Partners APIs
```typescript
GET    /api/partners?tenantId={id}   // عرض شركاء المستأجر
POST   /api/partners                // إضافة شريك جديد
```

### Wallets APIs
```typescript
GET    /api/wallets?tenantId={id}   // عرض محافظ المستأجر
GET    /api/wallets?branchId={id}   // عرض محافظ فرع
POST   /api/wallets                // إنشاء محفظة جديدة
// يتحقق تلقائياً من حدود الخطة
```

### Transactions APIs
```typescript
GET    /api/transactions?tenantId={id}   // عرض معاملات المستأجر
GET    /api/transactions?walletId={id}    // عرض معاملات محفظة
POST   /api/transactions                 // إضافة معاملة جديدة
// يتحقق تلقائياً من الصلاحيات
```

### Categories APIs
```typescript
GET    /api/categories?tenantId={id}   // عرض تصنيفات المستأجر
GET    /api/categories?type={type}     // تصفية حسب النوع
POST   /api/categories                // إنشاء تصنيف جديد
```

## 🔒 الأمان والخصوصية

- بيانات كل مستأجر معزولة تماماً
- التحقق من الصلاحيات في جميع الـ APIs
- التحقق من حدود الخطة عند إنشاء المحافظ
- لا يتم مشاركة أي بيانات بين المستأجرين
- تشفير الاتصالات بين الواجهة والخادم

## 📈 البيانات الأولية التجريبية

عند تشغيل `bun run db:seed`، سيتم إنشاء:

### المستأجر 1: خطة مجانية (FREE)
- 1 مستأجر (خطة مجانية)
- 1 شركة
- 2 فرع
- 4 مستخدمين (مالك، مدير فرع، مشرف، موظف)
- 1 شريك
- 2 محفظة (حد الخطة المجانية)
- 7 تصنيفات مالية
- 4 معاملات

### المستأجر 2: خطة التاجر (MERCHANT)
- 1 مستأجر (خطة التاجر)
- 2 شركات
- 3 فروع
- 5 مستخدمين (مالك، مدير شركة، مدير فرع، مشرف، موظف)
- 2 شريك
- 5 محافظ (غير محدودة)
- 10 تصنيفات مالية
- 5 معاملات

## 🎯 خارطة الطريق

### الميزات المستقبلية المخطط لها:
- [ ] نظام المصادقة الكامل (NextAuth.js)
- [ ] إدارة الصلاحيات التفصيلية
- [ ] تقارير متقدمة مع رسوم بيانية
- [ ] تصدير البيانات (PDF, Excel, CSV)
- [ ] ميزة الميزانية الشهرية
- [ ] إشعارات للمعاملات المهمة
- [ ] دعم لغات متعددة
- [ ] تخصيص كامل للسمات
- [ ] نظام الفواتير
- [ ] نظام تتبع الديون
- [ ] مزامنة عبر الأجهزة
- [ ] تطبيق موبايل منفصل
- [ ] بوابة دفع إلكترونية

## 🤝 المساهمة

للمساهمة في تطوير التطبيق:
1. Fork المشروع
2. أنشئ فرع للميزة الجديدة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push إلى الفرع (`git push origin feature/AmazingFeature`)
5. افتح Pull Request

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT - راجع ملف LICENSE للتفاصيل

## 🆘 الدعم

إذا واجهت أي مشاكل أو كان لديك أسئلة:
- افتح Issue على GitHub
- تواصل معنا عبر البريد الإلكتروني

---

تم التصميم والتطوير بواسطة ❤️ باستخدام Next.js و TypeScript و Prisma
