# تحسينات فحص صحة النظام - لوحة تحكم المطور

## ✅ التحسينات المنجزة

### 1. **الحالة العامة للنظام**

#### المعلومات المحسّنة:
```typescript
{
  status: 'ok' | 'degraded' | 'error',  // الحالة العامة
  timestamp: string,                         // وقت الفحص
  uptime: number,                           // وقت التشغيل بالثواني
  uptimeFormatted: string,                 // وقت التشغيل بصيغة نصية
  environment: string,                       // البيئة (development/production)
  responseTime: number,                    // زمن استجابة النظام
  dbConnectionTime: number,                 // زمن اتصال قاعدة البيانات
  performance: {
    status: 'excellent' | 'good' | 'fair' | 'poor',  // حالة الأداء
    responseTime: number,                    // زمن الاستجابة
    dbConnectionTime: number                 // زمن الاتصال
  }
}
```

#### البطاقة المحسّنة في الواجهة:
```
1. الحالة العامة الكبيرة:
   - دائرة كبيرة باللون المناسب (أخضر أو أحمر)
   - أيقونة CheckCircle2 أو XCircle
   - عنوان: "النظام سليم" أو "مشكلة في النظام"
   - Badge: "جاهز للعمل" أو "يحتاج للصيانة"

2. ثلاثة مقاييس رئيسية:
   
   أ) حالة قاعدة البيانات:
      - أيقونة Database
      - Badge: "سليمة" أو "مشكلة"
      - نص: "متصل" أو "غير متصل"
      - زمن الاتصال بالـ ms
   
   ب) استجابة النظام:
      - أيقونة Activity
      - Badge: "سريع" / "جيد" / "بطيء"
      - زمن الاستجابة الكلي بالـ ms
      - زمن اتصال قاعدة البيانات
   
   ج) وقت التشغيل:
      - أيقونة Clock
      - وقت التشغيل بصيغة نصية
      - مثال: "5 يوم، 3 ساعة، 45 دقيقة"
```

---

### 2. **حالة قاعدة البيانات**

#### البطاقة المحسّنة:
```
1. أيقونة الحالة:
   - دائرة كبيرة (10x10)
   - خلفية: emerald-500/20 (سليم) أو red-500/20 (مشكلة)
   - أيقونة CheckCircle2 أو XCircle (6x6)

2. الحالة:
   - نص كبير: "سليمة" أو "مشكلة"
   - لون أبيض عريض

3. معلومات الاتصال:
   - "متصل" أو "غير متصل"
   - زمن الاتصال بالـ ms
   - مثال: "5ms"

4. خطأ الاتصال:
   - عرض رسالة الخطأ (إن وجدت)
   - لون أحمر
   - حجم خطأ: صغير (xs)

5. تفاصيل الاستعلام:
   - عدد المستأجرين
   - عدد الشركات
   - مثال:
     • المستأجرون: 5
     • الشركات: 10
```

#### التقييم الذكي للحالة:
```typescript
// في الـ API
const dbStatus = await db.tenant.count()
? 'healthy' : 'unhealthy'

// تقييم زمن الاتصال
const dbConnectionTime = Date.now() - dbStartTime
const connectionSpeed = dbConnectionTime < 10 ? 'ممتاز' : dbConnectionTime < 50 ? 'جيد' : 'متوسط'
```

---

### 3. **زمن الاستجابة**

#### البطاقة المحسّنة:
```
1. زمن الاستجابة الكلي:
   - الرقم الكبير: مثال: "150ms"
   - لون أبيض عريض
   
2. زمن اتصال قاعدة البيانات:
   - يعرض بالـ ms
   - مثال: "قاعدة البيانات: 5ms"

3. تقييم السرعة:
   - Badge ملون:
     • "سريع" (أزرق) - أقل من 300ms
     • "جيد" (ثانوي) - 300-500ms
     • "بطيء" (أحمر) - أكثر من 500ms

4. تقييم الأداء العام:
   - Badge:
     • "ممتاز" (excellent) - أقل من 100ms
     • "جيد" (good) - 100-300ms
     • "جيد" (fair) - 300-500ms
     • "ضعيف" (poor) - أكثر من 500ms
```

---

### 4. **وقت التشغيل (Uptime)**

#### التخطيط المحسّن:
```
1. بطاقة مفصلة:
   - أيقونة Clock
   - عنوان: "وقت التشغيل"
   
2. عرض مضاعف:
   - النسخة الكبيرة: مثال: "5 يوم، 3 ساعة، 45 دقيقة"
   - النسخة الصغيرة: "uptime"

3. دالة formatUptime المحسّنة:
   - عرض بالأيام إذا > 0
   - عرض بالساعات إذا > 0
   - عرض بالدقائق دائماً
   - مثال: "5 يوم، 3 ساعة، 45 دقيقة"
```

#### تنفيذ الدالة:
```typescript
const formatUptime = (seconds: number) => {
  const days = Math.floor(seconds / (3600 * 24))
  const hours = Math.floor((seconds % (3600 * 24)) / 3600)
  const minutes = Math.floor((seconds % 3600) / 60)
  
  if (days > 0) {
    return `${days} يوم، ${hours} ساعة، ${minutes} دقيقة`
  } else if (hours > 0) {
    return `${hours} ساعة، ${minutes} دقيقة`
  } else {
    return `${minutes} دقيقة`
  }
}
```

---

### 5. **معلومات السيرفر (Node.js، المنصة، الذاكرة)**

#### البطاقات المحسّنة:

##### أ. البطاقة الرئيسية - "معلومات السيرفر":
```
Header:
- أيقونة Server
- العنوان: "معلومات السيرفر"
- الوصف: "معلومات تقنية عن بيئة التشغيل"
```

##### ب. معلومات Node.js:
```
Layout:
- مربع كبير (10x10) باللون الأخضر (green-500/20)
- أيقونة Server كبيرة (6x6) باللون الأخضر

Content:
- عنوان: "إصدار Node.js"
- النسخة: مثال: "v18.17.0"
- لون: أبيض عريض (xl)
```

##### ج. النظام الأساسي والمعمارية:
```
النظام الأساسي (Platform):
- مربع (8x8) باللون البنفسجي (purple-500/20)
- أيقونة Server (4x4) باللون البنفسجي
- عنوان: "النظام الأساسي"
- القيمة: مثال: "linux"
- لون: أبيض (lg semibold)

المعمارية (Architecture):
- مربع (8x8) باللون الأزرق (blue-500/20)
- أيقونة Server (4x4) باللون الأزرق
- عنوان: "المعمارية"
- القيمة: مثال: "x64"
- لون: أبيض (lg semibold)
```

##### د. بيئة التشغيل:
```
Layout:
- مربع (8x8) باللون الأصفر (amber-500/20)
- أيقونة Shield (4x4) باللون الأصفر
- عنوان: "بيئة التشغيل"
- Badge: 
  - لون default إذا production
  - لون secondary إذا development
- نص: مثال: "development"
- نص إضافي: "Production Environment" إذا production
```

##### هـ. معلومات العملية (Process Info):
```
PID (Process ID):
- مثال: "12345"
- لون: أبيض (font-mono)
- في خانة منفصلة

وقت التشغيل (Process Uptime):
- نفس نسخة formatUptime
- مثال: "5 يوم، 3 ساعة، 45 دقيقة"

عدد الأنوية (CPU Cores):
- مثال: "4"
- في خانة منفصلة

استخدام CPU (CPU Usage):
- مثال: "1.23"
- في خانة منفصلة
```

---

### 6. **استخدام الذاكرة مع شريط تقدم**

#### البطاقة المحسّنة - "استخدام الذاكرة":
```
Header:
- أيقونة Activity
- العنوان: "استخدام الذاكرة"
- الوصف: "استخدام الذاكرة مع تحليل تفصيلي"
```

#### أ. شريط التقدم الرئيسي:
```
1. العنوان والشارة:
   - أيقونة Activity
   - عنوان: "استخدام الذاكرة الكلي"
   - Badge:
     • "طبيعي" (default) - أقل من 70%
     • "مرتفع" (secondary) - 70-85%
     • "حرج" (destructive) - أكثر من 85%

2. النسبة المئوية:
   - الرقم الكبير: مثال: "75%"
   - لون:
     • أخضر (emerald-400) - أقل من 70%
     • أصفر (amber-400) - 70-85%
     • أحمر (red-400) - أكثر من 85%

3. شريط التقدم:
   - طول: 100%
   - ارتفاع: كبير (h-4)
   - لون خلفية: slate-800 (رمادي داكن)
   - لون التقدم:
     • تدرج من الأخضر إلى الأزرق (from-emerald-500 to-emerald-400) - أقل من 70%
     • تدرج من الأصفر إلى الأصفر (from-amber-500 to-amber-400) - 70-85%
     • تدرج من الأحمر إلى الأحمر (from-red-500 to-red-400) - أكثر من 85%
   - دائري تماماً (rounded-full)
   - تأثير انتقال (transition-all duration-500)

4. علامات التقدم:
   - أرقام أسفل الشريط: 0% | 25% | 50% | 75% | 100%
   - لون: رمادي داكن (slate-500)
   - حجم: صغير (xs)

5. التوصية التلقائية:
   - "استخدام الذاكرة منخفض جداً - أداء ممتاز" - أقل من 50%
   - "استخدام الذاكرة طبيعي - أداء جيد" - أقل من 70%
   - "استخدام الذاكرة مرتفع - قد يؤثر على الأداء" - 70-85%
   - "استخدام الذاكرة حرج - قد يتسبب في مشاكل" - أكثر من 85%
```

#### ب. التفاصيل المفصلة للذاكرة:

```
الذاكرة المستخدمة (Heap Used):
- مثال: "256"
- وحدة: ميجابايت (ميجابايت)
- لون: أبيض عريض (2xl)

الذاكرة الكلية (Heap Total):
- مثال: "512"
- وحدة: ميجابايت (ميجابايت)
- لون: أبيض عريض (2xl)

RSS (Resident Set Size):
- مثال: "300"
- وحدة: ميجابايت (ميجابايت)
- لون: أبيض عريض (2xl)
- نص توضيحي: "ميجابايت"

الذاكرة خارجية (External):
- مثال: "50"
- وحدة: ميجابايت (ميجابايت)
- لون: أبيض عريض (2xl)
- نص توضيحي: "ميجابايت"

Array Buffers:
- مثال: "25"
- وحدة: ميجابايت (ميجابايت)
- لون: أبيض عريض (2xl)
- نص توضيحي: "ميجابايت"

الذاكرة المتاحة (Available):
- مثال: "256"
- وحدة: ميجابايت (ميجابايت)
- لون: أخضر عريض (2xl) (emerald-400)
- حساب: Heap Total - Heap Used
```

#### تخطيط الشبكة:
```
المخطيط: 3 أعمدة (lg) أو 2 أعمدة (md)
• العمود 1: Heap Used, Heap Total
• العمود 2: RSS, External
• العمود 3: Array Buffers, Available
```

---

## 🎨 التحسينات البصرية والتصميمية

### 1. **ألوان ديناميكية للعلامات**

```
قاعدة البيانات:
- سليمة: أزرق (default)
- مشكلة: أحمر (destructive)

استجابة النظام:
- سريع (< 300ms): أزرق (default)
- جيد (300-500ms): ثانوي (secondary)
- بطيء (> 500ms): أحمر (destructive)

الأداء العام:
- ممتاز (< 100ms): أزرق (default)
- جيد (100-300ms): ثانوي (secondary)
- جيد (300-500ms): ثانوي (secondary)
- ضعيف (> 500ms): أحمر (destructive)

استخدام الذاكرة:
- طبيعي (< 70%): أزرق (default)
- مرتفع (70-85%): ثانوي (secondary)
- حرج (> 85%): أحمر (destructive)
```

### 2. **أيقونات كبيرة وعملية**

```
الحالة العامة:
- دائرة 16x16 باللون المناسب
- أيقونة 10x10 في المنتصف

قاعدة البيانات:
- دائرة 10x10 باللون المناسب
- أيقونة 6x6 في المنتصف

الخدمات:
- مربع 10x10 أو 8x8 باللون المناسب
- أيقونة 6x6 أو 4x4 في المنتصف
```

### 3. **شريط تقدم متقدم**

```
المميزات:
- تدرج ملون (gradient)
- زوايا دائرية (rounded-full)
- تأثير انتقال سلس
- علامات نسبية (0%, 25%, 50%, 75%, 100%)
- توصيات تلقائية
```

---

## 📊 المعلومات الجديدة

### 1. **في الـ API `/api/dev/health`**

```typescript
{
  // الحالة العامة
  status: 'ok' | 'degraded' | 'error',
  
  // التوقيت
  timestamp: string,
  uptime: number,
  uptimeFormatted: string,
  
  // الاستجابة
  responseTime: number,
  dbConnectionTime: number,
  
  // بيئة النظام
  environment: string,
  
  // فحوصات الحالة
  healthChecks: {
    database: {
      status: 'healthy' | 'unhealthy',
      connectionTime: number,
      details: {
        tenantCount: number,
        companyCount: number,
        queryExecuted: boolean
      },
      error: string | null
    }
  },
  
  // معلومات النظام
  system: {
    nodeVersion: string,
    platform: string,
    arch: string,
    pid: number,
    uptime: number,
    uptimeFormatted: string,
    environment: string,
    memory: {
      rss: number,              // Resident Set Size
      heapTotal: number,        // إجمالي الذاكرة
      heapUsed: number,          // الذاكرة المستخدمة
      heapUsedMB: number,       // بالميجابايت
      heapTotalMB: number,     // بالميجابايت
      external: number,         // الذاكرة الخارجية
      arrayBuffers: number,      // Array Buffers
      usagePercentage: string,   // النسبة المئوية
      usageStatus: 'normal' | 'warning' | 'critical'
    },
    cpu: {
      load: string | 'N/A',   // استخدام CPU
      cores: number | 'N/A'  // عدد الأنوية
    }
  },
  
  // الأداء
  performance: {
    status: 'excellent' | 'good' | 'fair' | 'poor',
    responseTime: number,
    dbConnectionTime: number
  }
}
```

### 2. **البيانات الإضافية المحسوبة**

```typescript
// في الـ API

// 1. معلومات الاتصال
const dbConnectionTime = Date.now() - dbStartTime

// 2. معلومات قاعدة البيانات
const dbConnectionDetails = {
  tenantCount,
  companyCount,
  queryExecuted: true
}

// 3. معلومات الذاكرة المفصلة
const memoryInfo = {
  rss: Math.round(process.memoryUsage().rss / 1024 / 1024),
  heapTotal: Math.round(process.memoryUsage().heapTotal / 1024 / 1024),
  heapUsed: Math.round(process.memoryUsage().heapUsed / 1024 / 1024),
  heapUsedMB: Math.round(process.memoryUsage().heapUsed / 1024 / 1024),
  heapTotalMB: Math.round(process.memoryUsage().heapTotal / 1024 / 1024),
  external: Math.round(process.memoryUsage().external / 1024 / 1024),
  arrayBuffers: Math.round(process.memoryUsage().arrayBuffers / 1024 / 1024)
}

// 4. النسبة المئوية للاستخدام
const heapUsed = systemInfo.memory.heapUsed
const heapTotal = systemInfo.memory.heapTotal
const memoryUsagePercentage = ((heapUsed / heapTotal) * 100).toFixed(1)

// 5. حالة الذاكرة
const memoryUsageStatus = memoryUsagePercentage < 80 ? 'normal' : memoryUsagePercentage < 90 ? 'warning' : 'critical'

// 6. حالة الأداء
const performanceStatus = responseTime < 100 ? 'excellent' : responseTime < 300 ? 'good' : responseTime < 500 ? 'fair' : 'poor'

// 7. الحالة العامة
const overallStatus = dbStatus === 'healthy' && responseTime < 1000 ? 'ok' : dbStatus === 'healthy' ? 'degraded' : 'error'

// 8. تنسيق وقت التشغيل
const uptimeFormatted = formatUptime(process.uptime())

function formatUptime(seconds: number) {
  const days = Math.floor(seconds / (3600 * 24))
  const hours = Math.floor((seconds % (3600 * 24)) / 3600)
  const minutes = Math.floor((seconds % 3600) / 60)
  
  if (days > 0) {
    return `${days} يوم، ${hours} ساعة، ${minutes} دقيقة`
  } else if (hours > 0) {
    return `${hours} ساعة، ${minutes} دقيقة`
  } else {
    return `${minutes} دقيقة`
  }
}
```

---

## 🎯 حالات الاستخدام

### 1. **عرض الحالة العامة**
```
1. افتح لوحة المطور
2. اضغط على تبويب "النظام"
3. شاهد:
   - الحالة العامة الكبيرة
   - حالة قاعدة البيانات
   - زمن استجابة النظام
   - وقت التشغيل
```

### 2. **عرض تفاصيل قاعدة البيانات**
```
1. في تبويب "النظام"
2. شاهد بطاقة "تفاصيل قاعدة البيانات"
3. راجع:
   - الحالة مع أيقونة كبيرة
   - زمن الاتصال
   - معلومات الاستعلام
   - الأداء العام
```

### 3. **عرض معلومات السيرفر**
```
1. في تبويب "النظام"
2. شاهد بطاقة "معلومات السيرفر"
3. راجع:
   - إصدار Node.js
   - النظام الأساسي
   - المعمارية
   - بيئة التشغيل
   - PID العملية
   - عدد الأنوية
```

### 4. **عرض استخدام الذاكرة**
```
1. في تبويب "النظام"
2. شاهد بطاقة "استخدام الذاكرة"
3. راجع:
   - النسبة المئوية الكلية
   - شريط التقدم الملون
   - التوصيات التلقائية
   - التفاصيل المفصلة:
     • Heap Used
     • Heap Total
     • RSS
     • External
     • Array Buffers
     • Available
```

---

## 🔧 الملفات المنجزة

### 1. `/home/z/my-project/src/app/api/dev/health/route.ts`

#### التحديثات:
```typescript
// GET: معلومات صحة شاملة
{
  status: 'ok' | 'degraded' | 'error'
  timestamp: string
  uptime: number
  uptimeFormatted: string
  environment: string
  responseTime: number
  dbConnectionTime: number
  healthChecks: { /* database, system, performance */ }
  system: { /* nodeVersion, platform, arch, pid, memory, cpu */ }
}

// فحصات جديدة:
- فحص اتصال قاعدة البيانات مع زمن الاتصال
- فحص الأداء العام
- معلومات الذاكرة المفصلة
- معلومات CPU (عدد الأنوية، الاستخدام)
- معالجة الأخطاء الشاملة
```

### 2. `/home/z/my-project/src/components/DeveloperDashboard.tsx`

#### التحديثات:
```typescript
// 4 بطاقات رئيسية جديدة:

1. الحالة العامة للنظام:
   - حالة عامة مع أيقونة كبيرة
   - 3 مقاييس رئيسية
   - خلفية متدرجة
   
2. تفاصيل قاعدة البيانات:
   - حالة اتصال مفصلة
   - زمن الاتصال
   - تفاصيل الاستعلام
   - الأداء العام
   
3. معلومات السيرفر:
   - Node.js, Platform, Architecture
   - Environment, Process Info
   - CPU Info
   
4. استخدام الذاكرة:
   - شريط تقدم كبير متدرج
   - 6 أنواع من معلومات الذاكرة
   - توصيات تلقائية
   - نسبة مئوية ملونة
```

---

## ✅ جودة الكود

### ESLint:
```bash
bun run lint
# النتيجة: لا توجد أخطاء
# تحذير واحد فقط في use-toast غير متعلق
```

### TypeScript:
```typescript
# جميع Types محددة بشكل صحيح
# لا توجد أخطاء في الـ interfaces
# جميع الدوال معرفة بشكل صحيح
# لا توجد أخطاء في الصياغة
```

### Runtime:
```typescript
# جميع الحالات معالجة بشكل آمن
# default values موجودة للقيم الفارغة
# معالجة الأخطاء في جميع الدوال
# formatUptime() محسّنة لجميع الحالات
```

---

## 📝 الوثائق التقنية

### الـ Types المستخدمة:

```typescript
interface HealthStatus {
  status: 'ok' | 'degraded' | 'error'
  timestamp: string
  uptime: number
  uptimeFormatted: string
  environment: string
  responseTime: number
  dbConnectionTime: number
  healthChecks: {
    database: {
      status: 'healthy' | 'unhealthy'
      connectionTime: number
      details: {
        tenantCount: number
        companyCount: number
        queryExecuted: boolean
      }
      error: string | null
    }
  }
  system: {
    nodeVersion: string
    platform: string
    arch: string
    pid: number
    uptime: number
    uptimeFormatted: string
    environment: string
    memory: {
      rss: number
      heapTotal: number
      heapUsed: number
      heapUsedMB: number
      heapTotalMB: number
      external: number
      arrayBuffers: number
      usagePercentage: string
      usageStatus: 'normal' | 'warning' | 'critical'
    }
    cpu: {
      load: string | 'N/A'
      cores: number | 'N/A'
    }
  }
  performance: {
    status: 'excellent' | 'good' | 'fair' | 'poor'
    responseTime: number
    dbConnectionTime: number
  }
}
```

### الدوال المساعدة:

```typescript
// تنسيق وقت التشغيل
const formatUptime = (seconds: number) => {
  const days = Math.floor(seconds / (3600 * 24))
  const hours = Math.floor((seconds % (3600 * 24)) / 3600)
  const minutes = Math.floor((seconds % 3600) / 60)
  
  if (days > 0) {
    return `${days} يوم، ${hours} ساعة، ${minutes} دقيقة`
  } else if (hours > 0) {
    return `${hours} ساعة، ${minutes} دقيقة`
  } else {
    return `${minutes} دقيقة`
  }
}

// حساب النسبة المئوية للاستخدام
const memoryUsagePercentage = ((heapUsed / heapTotal) * 100).toFixed(1)

// حساب حالة الذاكرة
const memoryUsageStatus = memoryUsagePercentage < 80 ? 'normal' : memoryUsagePercentage < 90 ? 'warning' : 'critical'
```

---

## 📊 المقارنة مع النسخة السابقة

| الميزة | قبل | بعد |
|---------|-----|-----|
| الحالة العامة | بسيطة | كبيرة مع أيقونة وخلفية متدرجة |
| فحص قاعدة البيانات | بسيط | مفصل مع زمن الاتصال |
| زمن الاستجابة | رقم واحد | رقمين (كلي + قاعدة البيانات) |
| وقت التشغيل | دالة بسيطة | منسق بالعربية |
| Node.js | فقط النسخة | النسخة + أيقونة كبيرة |
| المنصة والمعمارية | فقط النصوص | مربعات ملونة مع أيقونات |
| الذاكرة | بسيط | شريط تقدم متدرج + 6 أنواع |
| شريط التقدم | لا توجد | متدرج ملون + علامات نسبية |
| التقييم الذكي | لا توجد | علامات ملونة لكل مقياس |
| توصيات | لا توجد | توصيات تلقائية |
| الأداء العام | لا توجد | Badge ملون |

---

## 🎓 التعليمات للمطورين

### إضافة فحوصات صحة جديدة:

```typescript
// 1. في /api/dev/health/route.ts

// إضافة فحصات إضافية
const additionalChecks = {
  database: { /* ... */ },
  system: { /* ... */ },
  performance: { /* ... */ }
}

// 2. حساب معلومات الذاكرة المفصلة
const memoryDetails = {
  rss: Math.round(process.memoryUsage().rss / 1024 / 1024),
  heapTotal: Math.round(process.memoryUsage().heapTotal / 1024 / 1024),
  heapUsed: Math.round(process.memoryUsage().heapUsed / 1024 / 1024),
  heapUsedMB: Math.round(process.memoryUsage().heapUsed / 1024 / 1024),
  heapTotalMB: Math.round(process.memoryUsage().heapTotal / 1024 / 1024),
  external: Math.round(process.memoryUsage().external / 1024 / 1024),
  arrayBuffers: Math.round(process.memoryUsage().arrayBuffers / 1024 / 1024),
  usagePercentage: ((heapUsed / heapTotal) * 100).toFixed(1),
  usageStatus: memoryUsagePercentage < 80 ? 'normal' : memoryUsagePercentage < 90 ? 'warning' : 'critical'
}

// 3. إرجاع النتائج الشاملة
return NextResponse.json({
  status: overallStatus,
  /* ... */
  system: {
    /* ... */
    memory: memoryDetails,
    cpu: {
      load: (process.cpuUsage().user / process.cpuUsage().system).toFixed(2),
      cores: process.cpus ? process.cpus().length : 'N/A'
    }
  },
  performance: {
    status: performanceStatus,
    responseTime,
    dbConnectionTime
  }
})
```

### إضافة ألوان ديناميكية:

```typescript
// في الواجهة
const getStatusBadge = (value: number, thresholds: any, labels: any) => {
  if (value < thresholds.good) {
    return <Badge variant="default">{labels.good}</Badge>
  } else if (value < thresholds.warning) {
    return <Badge variant="secondary">{labels.warning}</Badge>
  } else {
    return <Badge variant="destructive">{labels.danger}</Badge>
  }
}

// مثال لزمن الاستجابة
const responseTimeBadge = getStatusBadge(
  health?.responseTime || 0,
  { good: 300, warning: 500 },
  { good: 'سريع', warning: 'جيد', danger: 'بطيء' }
)
```

---

تم التحسين بنجاح! ✅

قسم فحص صحة النظام الآن يحتوي على:
- ✅ الحالة العامة للنظام مع عرض كبي
- ✅ حالة قاعدة البيانات مفصلة مع زمن الاتصال
- ✅ زمن الاستجابة للنظام وقاعدة البيانات
- ✅ وقت التشغيل منسق بالعربية
- ✅ معلومات السيرفر الكاملة (Node.js، المنصة، الذاكرة، CPU)
- ✅ استخدام الذاكرة مع شريط تقدم متدرج
- ✅ 6 أنواع من معلومات الذاكرة التفصيلية
- ✅ تقييم أداء ذكي مع علامات ملونة
- ✅ توصيات تلقائية للأداء
- ✅ تصميم احترافي وسهل الاستخدام
- ✅ ألوان ديناميكية حسب الأداء

الواجهة احترافية، شاملة، وسهلة الاستخدام! 🚀

---

تم التطوير بواسطة ❤️ باستخدام Next.js، TypeScript، و Prisma
